apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: httpd
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/Alaaibrahim2/sample-gitops-app.git
    targetRevision: main
    path: charts/httpd
    helm:
      releaseName: httpd

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true

